% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/superintervalsr.R
\name{as_granges}
\alias{as_granges}
\title{Convert IntervalMap items result back to GRanges}
\usage{
as_granges(items_result, seqname = "chr1", valuename = "value")
}
\arguments{
\item{items_result}{Result from search_items()}

\item{seqname}{Chromosome name to assign (default: "chr1")}

\item{valuename}{Value name to assign (default: "value")}
}
\value{
GRanges object
}
\description{
Helper function to convert search_items results back to Bioconductor objects.
}
\examples{
# Create an IntervalMap and search for items
im <- IntervalMap(c(1, 10), ends = c(5, 15), values = c("gene1", "gene2"))
items <- search_items(im, 3, 12)

# Convert result back to GRanges
gr <- as_granges(items, seqname = "chr1", valuename = "gene_id")
print(gr)
}
