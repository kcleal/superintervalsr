% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/superintervalsr.R
\name{search_idxs.batch}
\alias{search_idxs.batch}
\title{Batch search for indices}
\usage{
search_idxs.batch(x, starts, ends)
}
\arguments{
\item{x}{IntervalMap object}

\item{starts}{Integer vector of query start positions (inclusive)}

\item{ends}{Integer vector of query end positions (inclusive)}
}
\value{
List of integer vectors, where each element contains the 1-based indices
  of intervals that overlap with the corresponding query range
}
\description{
Search for indices of overlapping intervals for multiple query ranges simultaneously.
This is more efficient than calling search_idxs multiple times.
}
\examples{
im <- IntervalMap()
add(im, 1, 10, "gene1")
add(im, 5, 15, "gene2")
add(im, 20, 30, "gene3")
build(im)

# Search multiple ranges at once
result <- search_idxs.batch(im, c(7, 25), c(12, 35))
# Returns list(c(1, 2), c(3))
}
